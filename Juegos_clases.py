import random   

class Adivinanzas():
    #Constructor 
    def __self__(self, tiempo, vidas, pistas):
        #Atributos 
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas
    # Metodos de la clase 
    def Adivinanzas_facil(self):
        self.vidas = 5 
        self.tiempo = 3
        self.pistas = 3 

    def Adivinanzas_medio(self):
        self.vidas = 3
        self.tiempo = 3
        self.pistas = 2

    def Adivinanzas_dificil(self):
        self.vidas = 1
        self.tiempo = 1 
        self.pistas = 2  

class Ahorcado():
    # Constructor 
    def __self__(self, tiempo, vidas, pistas):
        # Atributpos 
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas
    # Metodos de clase
    def Ahorcado_facil(self):
        self.vidas = 5 
        self.tiempo = 5 
        self.pistas = 3 

    def Ahorcado_medio(self):
        self.vidas = 3 
        self.tiempo = 3 
        self.pistas = 3 

    def Ahorcado_dificil(self):
        self.vidas = 1 
        self.tiempo = 2 
        self.pistas = 3 
     
class sopa_de_letras():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

    def sopa_de_letras_facil(self):
        self.vidas = 5 
        self.tiempo = 5 
        self.pistas = 3 

    def sopa_de_letras_medio(self):
        self.vidas = 3 
        self.tiempo = 3 
        self.pistas = 3 

    def sopa_de_letras_dificil(self):
        self.vidas = 1 
        self.tiempo = 1
        self.pistas = 2 

class Encuentra_la_logica():
     def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas
        
class Preguntas_sobre_python():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Preguntas_de_matematicas():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Criptograma():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Quizizz_cultura_unimetana():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Memoria_con_emojis():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas   

class Logica_buleana():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Palabra_mezcla():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Escoge_un_numero_entre():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas

class Final_boss():
    def __self__(self, tiempo, vidas, pistas):
        self.tiempo = tiempo 
        self.vidas = vidas
        self.pistas = pistas